-# in app/views/movies/show.html.haml
%h2 Details about #{@car.make} #{@car.model}

%ul#details
  %li 
    Body Type:
    = @car.body
  %li
    Comments:
    = @car.comments

= link_to 'Edit info', edit_car_path(@car)
= button_to 'Delete', car_path(@car), :method => :delete, :confirm => 'Are you sure?'
= link_to 'Back to car list', cars_path
